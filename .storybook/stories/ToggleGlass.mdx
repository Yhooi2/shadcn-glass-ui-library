import { Meta, Canvas, ArgTypes, Controls } from '@storybook/blocks';
import * as ToggleGlassStories from '../../src/components/ToggleGlass.stories';

<Meta of={ToggleGlassStories} />

# ToggleGlass

Glassmorphism toggle switch component for binary on/off states with smooth animations.

## Features

- ✨ **Smooth Animation** - 200ms slide transition
- 📱 **Touch Optimized** - 44×44px touch target
- ♿ **WCAG 2.1 AA** - Full accessibility support
- 🎨 **3 Sizes** - sm (32×16), md (44×24), lg (56×28)
- 🔄 **Controlled/Uncontrolled** - Flexible state management

## Installation

```bash
npx shadcn@latest add @shadcn-glass-ui/toggle
```

## Usage

<Canvas of={ToggleGlassStories.Default} />

```tsx
import { ToggleGlass } from '@/components/glass/ui/toggle-glass';

function Demo() {
  const [enabled, setEnabled] = useState(false);

  return (
    <ToggleGlass
      checked={enabled}
      onCheckedChange={setEnabled}
      label="Enable notifications"
    />
  );
}
```

## API Reference

<ArgTypes of={ToggleGlassStories} />

<Controls of={ToggleGlassStories.Default} />

## Sizes

| Size | Dimensions | Touch Target | Use Case |
|------|------------|--------------|----------|
| `sm` | 32×16px | 44×44px | Compact UIs, tables |
| `md` | 44×24px | 44×44px | Default, forms |
| `lg` | 56×28px | 56×56px | Prominent settings |

<Canvas of={ToggleGlassStories.AllSizes} />

## Examples

### Settings Panel

```tsx
function SettingsPanel() {
  const [settings, setSettings] = useState({
    notifications: true,
    darkMode: false,
    autoSave: true,
  });

  return (
    <div className="space-y-4">
      <ToggleGlass
        checked={settings.notifications}
        onCheckedChange={(checked) =>
          setSettings(prev => ({ ...prev, notifications: checked }))
        }
        label="Push Notifications"
      />

      <ToggleGlass
        checked={settings.darkMode}
        onCheckedChange={(checked) =>
          setSettings(prev => ({ ...prev, darkMode: checked }))
        }
        label="Dark Mode"
      />

      <ToggleGlass
        checked={settings.autoSave}
        onCheckedChange={(checked) =>
          setSettings(prev => ({ ...prev, autoSave: checked }))
        }
        label="Auto Save"
      />
    </div>
  );
}
```

### With Description

```tsx
<div className="flex items-center justify-between">
  <div>
    <h4 className="font-medium">Email Notifications</h4>
    <p className="text-sm opacity-70">
      Receive emails about your account activity
    </p>
  </div>
  <ToggleGlass checked={emailEnabled} onCheckedChange={setEmailEnabled} />
</div>
```

## Accessibility

- **Keyboard**: Space/Enter to toggle
- **ARIA**: `role="switch"`, `aria-checked`
- **Labels**: Always provide visible labels

## Related Components

- [CheckboxGlass](/?path=/docs/components-checkboxglass--docs) - For multi-select
- [ButtonGlass](/?path=/docs/components-buttonglass--docs) - For actions

View source: [toggle-glass.tsx](https://github.com/yourusername/shadcn-glass-ui/blob/main/src/components/glass/ui/toggle-glass.tsx)
